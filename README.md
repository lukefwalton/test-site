# test-site

A scratch pad for vibe coding and experimenting with web stuff.

## Getting started

Clone the repo, install dependencies when you add them, and run whatever dev server your stack uses.

```bash
git clone git@github.com:lukefwalton/test-site.git
cd test-site
```

Nothing here yet — have at it.
